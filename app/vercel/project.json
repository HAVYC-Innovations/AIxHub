{
  "name": "aixhub-workspace",
  "framework": "vite",
  "installCommand": "cd app/frontend && npm install",
  "buildCommand": "cd app/frontend && npm run build",
  "outputDirectory": "app/frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/app/backend/main.py" }
  ]
}
